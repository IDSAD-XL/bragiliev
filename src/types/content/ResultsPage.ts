import { IResultsContent } from '../../components/elements/Results'
import { IRegForm } from '../../components/elements/RegForm'

export interface IResultsPageContent {
  resultsBlock: IResultsContent
  regFormBlock: IRegForm
}
